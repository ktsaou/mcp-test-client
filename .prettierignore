dist/
coverage/
playwright-report/
test-results/
node_modules/
legacy/
package-lock.json
*.min.js

# Shell scripts and pre-bootstrap backups — prettier has no parser for them.
*.sh
*.bak

# Scratch outputs (the in-repo tmp/ tree is gitignored; this is belt-and-
# braces so prettier doesn't trip if anything lands outside tmp/).
tmp/

# Legacy: root-level snapshot dumps from past advisor runs (kept for the
# files moved into tmp/ before tmp/ existed).
/snap*.yml
/snap*.yaml
/snap*.md
/snapshot*.yml
/snapshot*.yaml
/snapshot*.md
