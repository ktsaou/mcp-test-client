# 2026-04-25 — recovering from v1.0 + adopting the maintainer framework

## 1. Working session opens

**Costa, relaying user reports — v1.0 fails the basics.** Seven points,
verbatim:

1. UX/styling primitive — no button styling, no selected-tab indicator,
   confusing.
2. "Saving forms is the worst UX ever" — `window.prompt()` for naming a
   canned request.
3. UI is tiny and hard to work with.
4. Screen real-estate badly split. Tools list takes 1/3 of screen width and
   isn't resizable.
5. Log is unmanageable: no nav between requests/responses, no copy-as-JSON
   per message, no save-to-file. JSON should respect newlines (the original
   pretty-printer did this; the port doesn't). On narrow screens timestamp +
   direction icon waste width.
6. No tooltips anywhere.
7. Tool descriptions are unreadable (truncated or hidden).

Costa's framing: "the application seems a throw away, not practical for any
reasonable use" and "you have failed".

**The expectations these violated.**

- That a "release" means the visible surface has been honestly evaluated by
  the maintainer as a user, not just `npm test && git tag`.
- That basic interaction patterns (tooltips, active states, modals over
  prompts, resizable panes, copy buttons) are table stakes, not "polish".
- That the maintainer makes decisions and judges quality — does not ship
  the first thing that compiles.

## 2. Decisions taken

- [DEC-000](../decisions/DEC-000-working-framework.md) — Adopt the
  Maintainer/Worker/Advisor three-mode framework, with live `maintainer/*`
  documentation. Codified in CLAUDE.md.
- [DEC-001](../decisions/DEC-001-mantine-v9.md) — Pick Mantine **v9** as the
  UI component library; stop hand-rolling primitives. Revised from initial
  v8 pick after the framework analyst flagged v8.3.18 as the last 8.x
  release.
- [DEC-002](../decisions/DEC-002-ux-advisor-mandatory.md) — UX-critic
  advisor sign-off is mandatory before merging any change to a user-visible
  surface.
- [DEC-003](../decisions/DEC-003-json-view-newlines.md) — Reimplement the
  JSON view to match the legacy newline-respecting renderer (ports the
  original `legacy/json-pretty-printer.js` behaviour properly).
- [DEC-004](../decisions/DEC-004-mantine-appshell-resizable.md) — Mantine
  `<AppShell>` for chrome, `react-resizable-panels` for inner splits;
  panels persist to localStorage.
- [DEC-005](../decisions/DEC-005-bundle-budget.md) — CI bundle-size
  tripwire at 350 KB gzipped, prevents the `@mantine/code-highlight` 300 KB
  trap and similar regressions.
- [DEC-006](../decisions/DEC-006-json-view-ships-ahead.md) — Land the JSON
  view rewrite ahead of the Mantine migration; merged in commit
  `d7f7fb2`.
- [DEC-007](../decisions/DEC-007-non-costa-issues-into-v1-1.md) — Modal
  a11y, real tabs, light-theme contrast, empty-state copy fold into v1.1
  scope; log virtualization deferred to v1.1.1.
- [DEC-008](../decisions/DEC-008-maintainer-restructure.md) — Split
  `product.md` / `feedback.md` / `decisions.md` into per-purpose
  subdirectories with a skills layer.

## 3. Advisors run

**UX-critic advisor pass on live v1.0.** Spawned the UX-critic template
(see [`../agents.md`](../agents.md)) against
https://ktsaou.github.io/mcp-test-client/. Verdict: **do not ship as
v1.x.y; wait for the v1.1 Mantine migration.** Full report at
`/tmp/ux-review-2026-04-25/` plus 22 screenshots.

§3 60-second flow — 1 pass / 2 partials / 3 rough-or-fail.

Four of Costa's seven items still visibly present in the deploy:

- JSON newlines ([DEC-003](../decisions/DEC-003-json-view-newlines.md)
  falsifier) — fixed in commit `d7f7fb2`, awaiting deploy.
- `window.prompt()` for canned-request naming — captured in v1.1 worker
  brief.
- Sidebar / tools-pane fixed widths — captured in v1.1 worker brief.
- Tool descriptions truncated and missing from request panel — captured.

Additional issues the critic surfaced beyond Costa's seven (folded into
v1.1 by [DEC-007](../decisions/DEC-007-non-costa-issues-into-v1-1.md)):

1. Modal a11y is broken — Add Server modal: Escape doesn't close, Enter
   doesn't submit, no focus trap, Tab leaks behind the backdrop. Mantine
   `Modal` fixes all four for free.
2. Tabs are not real tabs — no `role="tab"`, no `aria-selected`, no
   arrow-key nav. Mantine `Tabs` fixes for free.
3. Light theme has near-zero header contrast — title and `Connected` chip
   on pure-white. Mantine's auto-generated palette should resolve, verify
   post-migration.
4. Empty states are one-liners ("void") — each empty state needs a
   paragraph that helps the user know what to do next.
5. No log virtualization — possible perf cliff at 200+ messages. Tracked
   as a v1.1 risk to verify after migration.

Things the critic flagged as already-good — don't regress them in the
migration:

- Theme cycle (Dark → Light → System) with persisted state.
- Per-field schema form rendering with description + required marker.
  Mantine doesn't ship this shape; the bespoke `src/schema-form/` must
  stay.
- Share link compress/round-trip works — only a toast is missing.

The critic's prompt-improvement suggestion: in addition to "Costa reports
the same problems", test four scopes I had not specified — iPhone-size
viewport (390×844), share-link reload in a fresh incognito tab,
bearer-token auth flow, 200+ messages in the log (scroll perf). These are
now defaults in the UX-critic template in [`../agents.md`](../agents.md)
and codified as a guardrail in
[`../skills/ux-review.md`](../skills/ux-review.md).

**Framework-choice analyst sign-off on
[DEC-001](../decisions/DEC-001-mantine-v9.md).** Confirmed Mantine over
shadcn / Radix Themes / Chakra / Park, with a correction: **use v9, not
v8** (v8.3.18 is the last 8.x release). Surfaced four concrete migration
risks now captured in [DEC-001](../decisions/DEC-001-mantine-v9.md)
advisor sign-off: button hover regression on
React 19 (#8482), Radio deselect bug (#8461), `AppShell.Navbar` not
draggable (use `react-resizable-panels` for both splits),
`@mantine/code-highlight` bundle trap (~300 KB if mis-imported). Real
bundle estimate revised upward to 180–230 KB gzipped. CI tripwire at
350 KB added as [DEC-005](../decisions/DEC-005-bundle-budget.md).

## 4. Worker dispatched

**Mantine v1.1 migration brief.** A Worker is currently migrating
`src/ui/*.tsx` and `src/ui/*.css` to Mantine v9. Scope includes the
chrome (`<AppShell>`), `react-resizable-panels` for inspector-vs-request
and main-vs-log splits, real `Modal` for the canned-request naming flow,
real `Tabs`, tooltips on every actionable element, and empty-state copy
per [DEC-007](../decisions/DEC-007-non-costa-issues-into-v1-1.md).

Status at session close: **in flight.**

In parallel, the maintainer-side restructure to the new
`maintainer/` layout per
[DEC-008](../decisions/DEC-008-maintainer-restructure.md) was delegated to
a separate Worker (this commit) — file-scope-coordinated to stay out of
`src/` while the Mantine migration runs.

Earlier in the session, before the parallel split, the maintainer
personally landed:

- `d7f7fb2` — newline-respecting JSON view + per-message copy/download.
- `3406a82` — MantineProvider, postcss preset, theme bridge.
- `e57f757` — [DEC-005](../decisions/DEC-005-bundle-budget.md) CI
  bundle-size tripwire (350 KB gz cap).

These edits to `src/` and config were Maintainer-mode work that should
have gone to a Worker; see §5.

## 5. What I (the maintainer) learnt today

Costa's methodology feedback in this session became three guardrails:

1. **One file per working day for the log, plus a stable skills layer** —
   single growing files are forgetting at scale. Codified in
   [DEC-008](../decisions/DEC-008-maintainer-restructure.md). Guardrail
   landed in [`../log/README.md`](README.md) (this directory's contract)
   and in [`../skills/feedback-folding.md`](../skills/feedback-folding.md)
   (the routing rule for new feedback).
2. **Multiple skills with explicit guardrails** — when feedback fires,
   route it to the skill that should have prevented it; if no such skill
   exists, create one in the same edit. Codified in
   [`../skills/feedback-folding.md`](../skills/feedback-folding.md);
   today's seven-point report drove guardrail updates in
   [`../skills/release-readiness.md`](../skills/release-readiness.md),
   [`../skills/ux-review.md`](../skills/ux-review.md),
   [`../skills/advisor-spawn.md`](../skills/advisor-spawn.md), and
   [`../skills/ui-surface-change.md`](../skills/ui-surface-change.md).
3. **Delegation discipline** — Costa called this out directly: "you do
   work yourself instead of delegating." In this session I personally
   wrote the bundle-budget script, the CI tweak, and the json-view
   changes while a Worker was busy on `src/ui/`. Guardrail landed in
   [`../skills/delegation.md`](../skills/delegation.md): any file outside
   `maintainer/` and `CLAUDE.md` is now Worker territory by default;
   maintainer-mode source edits require a written justification in the
   daily log.

## 6. v1.1 candidate ready (commit `ec7f6ee`)

Both parallel Workers have landed.

**Mantine UI migration Worker.** Complete. All seven Costa items
addressed; four UX-critic-extra items (modal a11y, real tabs,
contrast, empty-state copy) addressed; three deviations flagged
(local-state Modal for naming over `openContextModal`; `resizer.tsx`
retained as dead code per no-delete rule; `mcptc:mcptc:ui.*`
double-prefix in localStorage keys preserved as out-of-scope).

Pipeline at the candidate commit: 131/131 tests, lint clean,
typecheck clean, build green. Bundle 265.7 KB gzipped (84 KB under
the [DEC-005](../decisions/DEC-005-bundle-budget.md) 350 KB cap).
Hover-state smoke check (DEC-001 risk #1) — Mantine
[#8482](https://github.com/mantinedev/mantine/issues/8482) did NOT
reproduce on React 19.2.5 + Mantine 9.1.x.

**Maintainer-restructure Worker.** Complete. Three growing files
deleted; 25 new files under the new `maintainer/` tree; all relative
links validated; CLAUDE.md and `agents.md` retargeted at the new
paths.

## 7. Pre-deploy gate

Per [`../skills/ui-surface-change.md`](../skills/ui-surface-change.md)
step 3: a UX-critic advisor pass against a local dev build is
mandatory before push. Dev server at `http://localhost:5173/`; UX
critic spawned. Awaiting verdict; no push until they sign off.

If the critic clears: push → deploy → re-run UX critic against the
prod URL within 24 hours per
[`../skills/ui-surface-change.md`](../skills/ui-surface-change.md)
step 6. If it regresses, revert.

If the critic flags issues: fix, re-spawn, do not push.

## 8. New user feedback — response metrics are first-class value

**Costa, relaying user reports.** Verbatim:

> the mcp server response size, duration, and estimated are important
> metrics for users.

**My reading of "estimated":** estimated token count when the response is
fed into an LLM. MCP responses exist to be consumed by LLMs; knowing how
big a `tools/call` result is in tokens is the deciding factor when a user
plans an integration (context-window budgeting). "Bytes" answers transport
cost; "duration" answers latency; "tokens" answers LLM cost. All three are
distinct and useful.

**The expectation it violated.** Browser-only MCP tools are how a developer
_evaluates_ a public server. Without these three metrics they can answer
"does it work" but not "is it usable in my system". The
[`../product/quality-bar.md`](../product/quality-bar.md) quality bar today
lists "JSON renders so a human can read it" but does not list "the user
can see how big / fast / costly the response is." Updating the bar in this
session.

**Where the fix lives.**

- [`../product/quality-bar.md`](../product/quality-bar.md) gains a new
  bullet: "Per-response metrics — bytes, duration, estimated tokens — are
  visible everywhere a response is shown."
- [DEC-009](../decisions/DEC-009-response-metrics.md) opens for the
  implementation choice (heuristic vs real tokenizer; where to surface).
  Status: decision-pending, advisor pass required before code is written.

**Release scoping.** v1.1 is gating on its UX-critic pass right now and
the migration is large. Adding scope mid-flight repeats the chaos pattern
[`../skills/release-readiness.md`](../skills/release-readiness.md) is
trying to prevent. **Decision: this ships as v1.1.1, the very next
release after v1.1 lands.** Worker brief will be drafted after v1.1's
critic clears and v1.1 is deployed.

**Falsifier.** If a user runs through the §3 flow on the v1.1.1 deploy
and cannot answer "how many tokens / how many bytes / how long" for the
last response without opening DevTools, the fix wasn't enough.

## 9. UX-critic verdict on the v1.1 candidate (dev build) — NO SHIP

Critic ran against `http://localhost:5173/` (commit `ec7f6ee`). Full report
at `/tmp/ux-review-2026-04-25-v11/` plus 35 screenshots.

**Verdict.** No-ship. Three blockers; everything else solid.

**Blockers (must fix before push):**

1. **Mobile viewport (390 × 844) is unusable.** Four panes share the
   width; REQUEST clips to "REQUES"; header subtitle overlaps title.
   `AppShell` `flex-direction` is set on `<main>` but the inner panel
   container ignores it. Needs a responsive collapse to a single-pane
   stacked layout below ~768 px (sidebar → drawer; inventory / request /
   log as tabs).
2. **Share link drops the tool call.** [`../values.md`](../values.md)
   promises "share a reproducible tool call". Today the link round-trips
   server registration only — no selected tool, no method, no args. Look
   at `src/share-url/encode.ts` (encodes them) vs
   `src/ui/share-url-loader.tsx` (only materialises the server, doesn't
   apply `tool` / `args` / `raw`). Fix is in the loader.
3. **Enter does not submit the Add Server modal.** Quality-bar item 8
   requires it. Mantine `Modal` ships focus-trap + Esc but Enter-submit
   needs a `<form onSubmit>` wrapper and a `type="submit"` primary
   button. Same pattern needed in the Save-as modal.

**Borderlines (track, do not block v1.1):**

- Empty-state copy is technically present but still one-liners — the
  [`../decisions/DEC-007-non-costa-issues-into-v1-1.md`](../decisions/DEC-007-non-costa-issues-into-v1-1.md)
  promise was "paragraph that helps".
- Light-theme log timestamps `rgb(149,149,149)` on white = ~2.85:1,
  fails WCAG AA. Borderline on quality-bar item 10.
- ARIA `valuemin (63) > aria-valuemax (35)` on the main/log separator —
  inverted, screen readers will report a broken range.
- Heap at 30 messages = 43 → 127 MB (+83 MB, ~2.7 MB/pair). At 200
  messages projects to ~270 MB. Not catastrophic; close. Already
  deferred to v1.1.1 by
  [`../decisions/DEC-007-non-costa-issues-into-v1-1.md`](../decisions/DEC-007-non-costa-issues-into-v1-1.md).

These four fold into v1.1.1 alongside the response-metrics work
([DEC-009](../decisions/DEC-009-response-metrics.md)).

**What already feels good — don't regress.** Newline-respecting JSON
view with `↵` glyphs ([DEC-003](../decisions/DEC-003-json-view-newlines.md)
falsifier passes); per-message log hover actions with `aria-label`s;
real `role="tab"` tabs with arrow-key nav; modal focus-trap; bearer
token round-trips correctly into `Authorization` header.

**Critic's prompt-improvement note.** I should have asked them to verify
**share-link state-restoration scope** explicitly: `(a)` server only,
`(b)` server + selected tool, `(c)` server + tool + method + args. The
current implementation silently shipped (a) while the values.md promise
is (c). **Guardrail:** updated [`../skills/ux-review.md`](../skills/ux-review.md)

- the UX-critic template in [`../agents.md`](../agents.md) to spell out
  share-link scope on every future pass.

## 10. Worker brief — v1.1 blockers

[DEC-010](../decisions/DEC-010-v1-1-blockers.md) opens with the
direction. Worker brief drafted; spawning next.

After the Worker delivers: re-run the UX critic on the dev server
(same prompt) → if all three blockers clear, push → re-run critic
against the prod URL within 24 hours per
[`../skills/ui-surface-change.md`](../skills/ui-surface-change.md) §6.

## 11. Second UX-critic pass — NO SHIP again

Critic re-ran against `http://localhost:5173/` (commit `690ed7c`). Full
report at `/tmp/ux-review-2026-04-25-v11-rerun/` plus 42 screenshots.

**Verdict.** No-ship. One DEC-010 blocker only partially closed; two
new issues found, one of them brand-new at desktop width.

**Per-DEC-010-blocker resolution:**

- **Blocker 1 — mobile collapse: PARTIAL.** The panel-area collapse
  works (Drawer + Tabs as designed), but the header `<Group>` was not
  fixed. Below ~470 px width, the header `wrap="wrap"` puts
  Connect/Disconnect + theme-toggle on a _second_ row at `y: 50–86`
  while the bezel is only 0–56 px tall — so they overlay the tab
  strip at `y: 56–91`. Critic verified with
  `document.elementFromPoint(50, 75) → <Connect>` and a force-click
  on the Inventory tab triggering the underlying Disconnect button.
  §3 step 4 (click a tool) is unreachable on iPhone-size, so the
  original DEC-010 #1 falsifier ("§3 flow does not break") still
  fails. The patch was scoped to "panel collapse" not "header layout
  at narrow widths".
- **Blocker 2 — share-link state restoration: CLOSED.** Server + tool
  - method + arg values reconstruct in a fresh incognito context after
    the user clicks Connect. `inputValues: ["llama", "", "20"]` after
    reload-then-Connect on a `hub_repo_search` link.
- **Blocker 3 — Enter-submit: CLOSED on all three modals.** Add Server,
  Edit Server, Save-as. `<form onSubmit>` + `type="submit"` works
  uniformly.

**New issues found (this pass):**

- **N1 — `Send` and `Share` truncate to "Se" / "Sh" at 1440 px after
  the first Save-as.** Brand-new regression. The Save-as flow adds a
  "Load saved (n)" dropdown to the request-panel toolbar; the flex
  squeeze maims the primary actions. Severity: **high** — `Send` is
  the literal call-to-action of the 60-second flow.
  `45-truncated-buttons.png`.
- **N2 — Inventory empty-state lies on connect-error.** When connection
  fails (e.g. invalid bearer), header shows red `ERROR` chip but the
  inventory pane reads "Connect to a server to see its inventory."
  The user _just_ connected and was rejected; the message should
  surface the rejection, not pretend they haven't tried.
- **N3 — Save-as has no confirmation toast.** A localStorage write
  occurs with only a subtle "Load saved (1)" dropdown appearing as
  feedback. Fails quality-bar item 8 ("toasts confirm side effects").

**Borderlines (still deferred to v1.1.1, confirmed not regressed):**
empty-state one-liners; light-theme timestamp contrast 2.85:1; ARIA
range-inversion on the third separator; no log virtualization (heap
395 MB at 216 messages, tab still responsive).

**Critic's prompt-improvement note.** I framed the mobile DEC-010 fix as
"panel collapse" only. The critic's first pass had also been narrow,
implicitly assuming the header was fine because it didn't have any
overlapping content at 1440 px. **Guardrail:** the mobile-scope
checklist now demands verifying the header bar fits on a single row
at 360, 390, and 414 px widths in both Idle and Connected states,
and that nothing in the header overlays the panel area. Updated
[`../skills/ux-review.md`](../skills/ux-review.md) and
[`../agents.md`](../agents.md) accordingly.

**Things that already feel good (don't regress):** newline-respecting
JSON view with `↵` glyphs; share-link round-trip + toast; modal a11y
(`<form>` + `type="submit"`, Esc, focus-trap, tooltips).

## 12. v1.1 round-2 patch — DEC-011

Direction lives in
[`../decisions/DEC-011-v1-1-round-2.md`](../decisions/DEC-011-v1-1-round-2.md).
Worker brief drafted next.

After the Worker delivers: re-spawn the UX critic against the dev
build (third pass, _same scope_). Push gated on critic clearing all
four findings (B1 + N1 + N2 + N3). Borderlines stay deferred.

## 13. Third UX-critic pass — SHIP (conditional)

Critic ran against `http://localhost:5173/` (commit `eb1d3b6`). Full
report at `/tmp/ux-review-2026-04-25-v11-rerun3/`.

**Verdict.** **Ship.** All four DEC-011 findings closed against their
explicit falsifiers; DEC-010 Blockers 2 and 3 not regressed; deferred
borderlines unchanged.

**Per-finding resolution:**

- **F1 — mobile header.** Closed. At 360/390/414/480 px in Idle,
  Connected, and Error states: header height = 56 px exactly; no
  child has `bottom > 56`; `elementFromPoint(50, 75)` returns the
  Inventory tab label, not a header button. Compact chip set
  (`[burger][status-pill][primary][theme-toggle]`) renders below
  ~479 px as designed.
- **F2 — Send/Share at 1440 px after Save-as.** Closed. After Save-as
  at 1440 px: `Send` 70/69 (offset/scroll), `Share` 60/58, `Save as…`
  79/77 — all full-label. Verified at 1024 / 1280 / 1440 / 1600 px.
- **F3 — inventory empty-state on connect-error.** Closed. Four-branch
  state (`idle` / `connecting` / `error` / `connected & empty`)
  implemented. `error` shows "Server returned: <message>" + a hint;
  the misleading "Connect to a server" copy never renders when the
  status is error.
- **F4 — Save-as toast.** Closed. `Saved request "X"` and
  `Deleted "X"` toasts both fire bottom-right with default Mantine
  timing.

**DEC-010 regression check:**

- Blocker 2 (share-link state): not regressed. Server + tool + method
  - arg values all reconstruct in a fresh incognito tab.
- Blocker 3 (Enter-submit): not regressed. Add Server, Edit Server,
  Save-as all submit on Enter; Esc still closes.

**Minor items the critic flagged as NOT blockers** (track for v1.1.1):

- **F1 off-by-one at 480 px.** Spec said "below ~480 px"; the
  implementation is `< 479`. At exactly 480 px the title still wraps
  and Active/pill/buttons visually clip — but the F1 falsifier
  strictly passes (no overlay into the panel). Cosmetic; trivial fix
  to widen the predicate to `< 480`.
- **Delete-saved opens a Menu first.** Two-step affordance (Menu
  → confirm) is sensible for picking which entry to delete, but the
  trash icon's tooltip suggests a single-step confirm. Could collapse
  to a direct confirm when `entries.length === 1`.
- **Friendly name discarded on share-link reload.** Recipient sees
  the bare host instead of the sender's curated name. Cosmetic; would
  fit in the share payload encoding for ~10 bytes.

These three land in v1.1.1 alongside the deferred borderlines and
[DEC-009](../decisions/DEC-009-response-metrics.md) response metrics.

**Prompt-improvement note from critic.** The `verify header at 480 px`
guardrail in [`../skills/ux-review.md`](../skills/ux-review.md) and
[`../agents.md`](../agents.md) is ambiguous: at the boundary it could
mean "header doesn't overlay the panel" (strict falsifier — I read it
this way) or "compact mode is active" (DEC-011 spec intent). The two
disagree at exactly 480 px. **Guardrail update:** rewrite the
mobile-scope template wording to spell out both criteria so the next
pass can't disagree with itself.

**DECs closed by this pass:** [DEC-010](../decisions/DEC-010-v1-1-blockers.md)
(all three blockers; round 1 mobile partial → DEC-011 round 2 closed it),
[DEC-011](../decisions/DEC-011-v1-1-round-2.md) (all four findings).

## 14. Pushing v1.1 to prod

Conditional sign-off in hand. Committing the closing log entry +
status updates, then pushing the eight-commit chain. Live deploy
fires automatically. Within 24 hours: re-run the UX critic against
the prod URL per
[`../skills/ui-surface-change.md`](../skills/ui-surface-change.md) §6.
If anything regresses there, revert.

## 15. Prod critic: regression-free; CI E2E selector drift

Prod UX critic on `https://ktsaou.github.io/mcp-test-client/`: **prod
regression-free.** Bundle hash matches the dev pass; all four DEC-011
findings + both closed DEC-010 blockers verified on the live build;
§3 60-second flow works end-to-end on prod desktop. No CDN / CSP /
base-path / mixed-content surprises. Full report at
`/tmp/ux-review-2026-04-25-v11-prod/` (21 screenshots).

CI's E2E job, on the other hand, failed: the smoke test was waiting
on `locator('.pill--ok')`, which v1.0 used as the connected-state
chip class. The Mantine migration replaced it with a Mantine `Badge`
that has no such class. Test-only drift, not a behaviour bug.

**Maintainer-mode minor patch (justification, per
[`../skills/delegation.md`](../skills/delegation.md))**: this is a
single line in a test file fixing locator drift after the migration;
no production code change; spawning a Worker for a one-line locator
fix is more overhead than value. New locator:
`page.getByText('Connected', { exact: true })` — text-based, stable
across future migrations of the chip rendering.

Verified locally: `npx playwright test --project=chromium tests/e2e/smoke.spec.ts`
passes in 1.4 s. Push next; CI should now go green on master.

## 16. v1.1 release process

- Push the locator fix.
- After CI clears: tag `v1.1.0` and publish the GitHub release.
- Mark the v1.1 work complete; v1.1.1 work (DEC-009 metrics + the
  borderlines + the three minor critic notes) starts in a future
  session against the v1.1 baseline.

## 17. Costa: "did you address them all?" — no, three are open

Costa called out five items by example:

1. Request/response size + estimated tokens + duration metrics.
2. Painful log scroll — users need prev/next request navigation.
3. Copy any request/response from the log as the original raw JSON.
4. Timestamp + direction icon push log content right, wasting width.
5. **(NEW)** Each request needs a bold method summary line; per-entry
   collapse/expand; global collapse-all / expand-all so users can
   eyeball the request flow.

**Status, honest:**

- **#1 metrics** — deferred to v1.1.1, already logged as
  [DEC-009](../decisions/DEC-009-response-metrics.md). Deliberate.
- **#2 prev/next nav** — **MISSED.** Was buried inside the original
  "log is unmanageable" bullet of the 2026-04-25 seven-point report.
  I split that bullet into sub-items and addressed _copy_, _save_,
  _newlines_ — but didn't track _nav_ as its own line item. The
  v1.1 release notes claim "log is unmanageable" was closed, which
  was overclaim by exactly this much.
- **#3 copy from log** — implemented in v1.1 (`d7f7fb2`), hover-only.
  Real functionality, poor discoverability. Treat as "implemented
  but not user-visible" → ship a more obvious affordance in v1.1.1.
- **#4 timestamp / direction wasting width** — first UX-critic pass
  flagged this; I marked it a v1.1.1 borderline. **MISSED for v1.1.**
- **#5 method summary + collapse/expand** — completely new, not in any
  prior feedback. Now scoped.

**Framework failure to learn from.** I treated your original
"log is unmanageable" line as if it were one feature with four
mechanical fixes. It was actually four distinct user needs, two of
which slipped because I never wrote them down individually. The skill
guardrail folds in next.

**Scope for v1.1.1 — "the log, fixed"** — see
[`../decisions/DEC-012-v1-1-1-log-redesign.md`](../decisions/DEC-012-v1-1-1-log-redesign.md).
DEC-009 metrics consume the new row design rather than ship as a
separate concern. Worker brief drafts next.

**Quality-bar updates** (in [`../product/quality-bar.md`](../product/quality-bar.md)):

- Per-message copy/save must be **always-visible**, not hover-only.
- Each request entry has a method-summary headline that the user can
  scan without expanding the body.
- Per-entry collapse/expand exists; global collapse-all/expand-all
  exists.
- Prev/next request navigation exists in the log toolbar; keyboard
  shortcuts work.
- Timestamp + direction live inline in the headline row, not as a
  fixed-width left column.

## 18. Skills update: feedback-folding now splits multi-part bullets

The miss on #2 and #4 above came from treating one feedback bullet as
one line item to track. **Guardrail added to
[`../skills/feedback-folding.md`](../skills/feedback-folding.md):**
when a feedback bullet contains multiple "no X / no Y / no Z" claims,
the routing is one tracker per claim, not one tracker per bullet. The
DEC that resolves them must include a checklist of _each_ sub-item.

## 19. v1.1.1 dev critic — conditional ship; one spec violation, patch required

Critic against `http://localhost:5173/` returned conditional ship. Full
report in 45 screenshots at `/tmp/ux-review-2026-04-25-v111/`.

**13-row checklist:** 12.5 / 13 strict pass.

The half-fail is on the `notifications/initialized` row: it carries the
collapsible role + `aria-expanded="false"` + `tabindex="0"` (suggesting
it's expandable) but renders **no body** and **no copy / save / pair
buttons**. That breaks rows 7, 8, and 12 of DEC-012 strictly: copy and
save are not "always visible per entry"; collapse/expand "default
collapsed" is technically met but the row has nothing to collapse to or
expand from. Critic suggests two paths: classify as a system message
(strip the collapsible chrome, use `•` glyph) **or** treat as a real
wire entry with a body. I prefer the latter — a JSON-RPC notification
**is** wire traffic and the recipient might want to copy or save it.

**Minor items the critic flagged as non-blocking:**

- At 360 px the response-row discriminator collapses to ellipsis
  because the metric chips dominate the row. Headline-truncates-
  gracefully is technically met. Cosmetic; deferred to v1.1.2.
- "Expand all" on 241 entries blocks ~3.7 s synchronously. Already
  accepted by DEC-012's perf note; resolves naturally when log
  virtualization lands.

**Regression check on v1.1:** all four items still pass — mobile
collapse + header at 360/390/414/480, share-link reload, modal
Enter-submit, newline-respecting JSON view.

**Mandatory scopes:** all pass; 200+ message stress at 241 entries
shows ~309 MB heap (consistent with the v1.1 baseline at 200 entries,
not regressed); rapid scroll ~50 ms/jump.

**Critic prompt-improvement notes** (folding into the UX-critic skill +
template next turn):

1. "Per entry" in DEC-012 should enumerate the entry kinds in scope:
   request, response, outgoing notification, outgoing JSON-RPC error,
   incoming server notification, system info/warn/error. The
   ambiguity around `notifications/initialized` came from this gap.
2. Mobile chip-vs-discriminator competition needs an explicit
   "below ~400 px chips wrap to a second visual row before the title
   is truncated past the method name" line.
3. Tokenizer drift between log-row and Last-result chips is expected
   (independent recomputations) and should be either labelled
   identically or noted in spec.

**Patch direction.** [DEC-013](../decisions/DEC-013-notification-row-consistency.md)
opens narrowly: `notifications/*` outgoing rows render as full
collapsible entries with a body (the notification message) and
copy/save buttons. Skills + template guardrails fold in alongside.

## 20. v1.1.1 second critic — SHIP

Re-pass on `http://localhost:5173/` (commit `8305809`): 19/19 rows
clear. DEC-012 13/13, DEC-013 6/6. Two DEC-013 rows interpreted from
classifier + unit tests (`notifications/cancelled` / `progress`
outgoing, and `list_changed` / `message` incoming) because the live
server doesn't elicit them in a basic session — explicit falsifier
disclosure in the report.

**Bonus regression-positive:** heap at 232 entries dropped from ~309
MB (previous pass at 241 entries) to ~101 MB. Likely cause: bodies
default-collapsed → fewer warm tokenizer instances. Not a regression
but worth knowing; **the prior log virtualization deferral becomes
less urgent** because the dominant memory cost (tokenized expanded
bodies) is paid lazily only when the user expands.

**Critic prompt-improvement note** (folding into agents.md +
skills/ux-review.md):

- Add a dev-only URL flag (e.g. `?mock=notifications-zoo`) to the
  dev build that synthesises one of each notification kind so future
  critics can drive rows 2 and 3 of DEC-013 live without
  interpretation. Tracked as a separate v1.1.x dev-tooling task.

**Closures:**

- [DEC-012](../decisions/DEC-012-v1-1-1-log-redesign.md) — closed (13/13).
- [DEC-013](../decisions/DEC-013-notification-row-consistency.md) — closed
  (6/6, with two rows interpreted via unit tests).
- [DEC-009](../decisions/DEC-009-response-metrics.md) — closed by being
  subsumed into the DEC-012 row design (chips on every response row).

**Borderlines that stay deferred (now to v1.1.2):**

- 360 px discriminator clipped to ellipsis when chips dominate.
- Expand-all 3 s blocking on 200+ entries (DEC-012 perf note).
- Light-theme log timestamp contrast 2.85:1.
- Last-result vs log-row tokenizer drift labelling.
- Add-server invalid-placeholder edge case ("https://" submitted).
- Three minor critic items from the v1.1 cycle (480 px off-by-one,
  delete-saved Menu when one entry, friendly-name in share payload).
- Dev-only `?mock=notifications-zoo` flag for future critic passes.

## 21. Pushing v1.1.1 to prod

Push next; CI + Deploy fire automatically. Within 24 h: re-run UX
critic against prod URL per
[`../skills/ui-surface-change.md`](../skills/ui-surface-change.md) §6.
